# Backend Configuration
NEXT_PUBLIC_BACKEND_URL=https://madadwala-backend.vercel.app

# Note: All other environment variables should be set in your backend
# The frontend only needs the backend URL to connect to your Express API
